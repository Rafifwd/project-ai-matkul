# Backend API base URL.
# In production (Vercel), leave this UNSET — the frontend and backend share the same
# origin so all /api/* requests go to the same domain automatically.
# In local development, set this to the running backend address.
VITE_API_URL=http://127.0.0.1:8000
