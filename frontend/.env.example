# Backend API base URL.
# In production (Vercel) AND local development: leave UNSET (empty string).
# The Vite dev server proxies /api/* → http://127.0.0.1:8000/* automatically.
# In production, Vercel routes /api/* to the backend service with the same stripping.
VITE_API_URL=